# Code review findings — 2026-07-02

Tracking doc for the July 2026 code review. Update the status column as items
get fixed. Statuses: **Fixed** (with date) / **Open**.

## Bugs & correctness

| # | Finding | Where | Status |
|---|---------|-------|--------|
| 1 | Services never checked `response.ok`: deletes reported success on 401/403/500, and JSON error bodies were handed to callers as if they were entities (e.g. `AddLocation` could navigate to `/locations/undefined`). | all of `src/services/` | **Fixed 2026-07-02** — all services now go through `src/services/apiClient.js`, which checks `response.ok` and returns the sentinel on any failure. Deletes return real success booleans. |
| 2 | `ProtectedRoute` `requiredRoles` compared role *objects* to name strings (`requiredRoles.includes(roles[i])`), so any route using `requiredRoles` would deny everyone. | `src/components/route/ProtectedRoute.jsx` | **Fixed 2026-07-02** — matches on `role.name` via `.some()`. |
| 3 | CI broken: `actions/upload-artifact@v2` / `download-artifact@v2` were shut down by GitHub (early 2025); Vitest exits non-zero with zero test files; Node 18 is EOL; multi-line pwsh `run:` only propagated the last command's exit code. | `.github/workflows/master_fishingmap.yml` | **Fixed 2026-07-02** — checkout/setup-node/artifact actions bumped to v4, Node 20, install/build/test split into separate steps, starter tests added (`apiClient.test.js`, `geoUtils.test.js`). Note: artifact v4 excludes dotfiles, so `.env.production` is no longer deployed — it's only needed at build time (Vite bakes values into the bundle). |
| 4 | `authService.login` sent `'Content-Type': ' application/json'` (leading space — invalid media type). | `src/services/authService.jsx` | **Fixed 2026-07-02** — as part of the apiClient migration. |
| 5 | Failed login gives no user feedback: `Login.handleSubmit` does nothing when `authService.login` returns false. | `src/pages/login/Login.jsx` | **Fixed 2026-07-02** — failed login (and a failed whoami after login) sets a form-level error via Formik status, rendered by `LoginForm`. Verified in the running app against a real 401 and an unreachable backend. |
| 6 | Stale session never re-validated: `sessionStorage` user is trusted even after the auth cookie expires; user looks logged in while every action fails silently. Also `JSON.parse` of the stored user is unguarded — a corrupted value white-screens the app (provider gates all children). | `src/context/CurrentUserContext.jsx` | **Fixed 2026-07-02** — provider restores optimistically, then re-validates via new `authService.getSession()` (whoami): 401/403 signs out, success refreshes the stored user (and restores sessions in new tabs), unreachable backend keeps the stored user so a network blip doesn't sign anyone out. `JSON.parse` is guarded and corrupt entries are removed. New `apiClient.requestResult` helper distinguishes HTTP rejection from network failure. |
| 7 | `EditMediaPanel` fetches every image as a blob just to preview it (plain `<img src>` to `VITE_IMAGES_URL` would do), re-downloads all images after every save (effect depends on `location`, and save calls `onLocationUpdated`), and never revokes its `URL.createObjectURL` results. Same pattern in `EditSpeciesMediaPanel`. | `src/pages/location/edit/panels/EditMediaPanel.jsx`, `src/pages/species/edit/panels/EditSpeciesMediaPanel.jsx` | **Fixed 2026-07-02** — both panels replaced by shared `src/components/ui/media/MediaManagerPanel.jsx` (see S2). Existing images render directly from the images endpoint via `fileService.getImageUrl`; object URLs are only created for staged uploads and revoked on cancel/save/unmount. Verified in the running app: zero blob-download fetches, staging/cancel/failed-save all behave. |
| 8 | `ImportGeoJson` throws when the file dialog is cancelled (`files[0]` undefined) and never resets `input.value`, so re-importing the same file does nothing. | `src/components/ui/form/ImportGeoJson.jsx` | **Fixed 2026-07-02** — no-file guard added, and the input value is reset after each read so the same file fires onChange again. Verified in the add-location wizard: empty selection produces no error, and a valid import flows through to Formik (Next advances past the Map step). |
| 9 | `RangeInput` only commits on `mouseUp`/`touchEnd` — keyboard users can never apply the distance filter. | `src/components/ui/form/RangeInput.jsx` | **Fixed 2026-07-02** — commits on keyup for slider keys (arrows/Home/End/PageUp/PageDown). Verified keyboard-only on /locations: ArrowRight → `?distance=1`, End → `?distance=100`, Home → param cleared. |
| 10 | Distance filter silently ignored without geolocation: `radius` is only sent when `radiusOrigin` exists, so users who denied location permission drag the slider with no effect and no explanation. | `src/services/locationService.jsx` + `src/pages/location/list/Locations.jsx` | **Fixed 2026-07-02** — `RangeInput` gained `disabled`/`hint` props; without geolocation the Locations distance slider renders disabled with "Allow location access to filter by distance". Verified with and without a mocked geolocation. |
| 11 | `DragAndDropImage`: dedupes files by name only; uses Formik `setError` for validation messages (wiped on next validation pass); hidden input lacks `accept`. | `src/components/ui/form/DragAndDropImage.jsx` | **Fixed 2026-07-02** — dedupe by name+size+lastModified, validation message in local state, `accept` from shared `src/constants/images.js`. **Caveat:** the component turned out to be dead code (see S14); the *live* uploaders (`StepPhotos`, `AddSpecies` inline grid) got the equivalent fixes — shared type list, explicit `accept`, and object-URL revocation on remove/unmount. Verified on /species/add. |
| 12 | Detail pages render a blank page when the entity fetch fails/404s (spinner disappears, nothing replaces it). No "not found" state. | `LocationDetails`, `SpeciesDetails`, `PermitDetails` | **Fixed 2026-07-02** — shared `src/components/ui/notFound/NotFoundMessage.jsx` wired into all three detail pages with a back-to-list link. Verified against real backend 404s on all three routes. |
| 13 | `Pagination` uses `<a href="/#">` for buttons — middle-click/ctrl-click navigates to `/#`; accessibility miss. | `src/components/ui/pagination/Pagination.jsx` | **Fixed 2026-07-02** — real `<button type="button">` elements with `aria-current="page"` on the active page and value-based keys. Verified on /locations: zero anchors, page 2 click updates URL + list. |
| 14 | Maps API key: any `VITE_` key ships in the public bundle, so the control that matters is Google Cloud Console restriction (HTTP referrer locked to fishingmap.fi + API restriction to Maps JavaScript API). Verify this is configured. | `.env.production` / Google Cloud Console | Open (verify) |

## Structure / maintainability

| # | Finding | Status |
|---|---------|--------|
| S1 | No shared fetch wrapper — ~30 copies of the same try/fetch/json/catch block across six services. | **Fixed 2026-07-02** — `src/services/apiClient.js` (getJson / sendJson / sendForm / requestOk). |
| S2 | `EditMediaPanel` (262 lines) and `EditSpeciesMediaPanel` (258 lines) are near-clones — extract a shared `MediaManagerPanel` taking `addImage`/`removeImage`/`refetch` callbacks. | **Fixed 2026-07-02** — `src/components/ui/media/MediaManagerPanel.jsx` (+ scss) with `images`/`maxImages`/`addImage`/`removeImage`/`refetch`/`onSaved` props; both edit pages wire it to their own service. Old panels and their SCSS clones deleted. Bonus: the five ad-hoc `${VITE_IMAGES_URL}/${path}` template strings across list/details/map components now use `fileService.getImageUrl`, so the images origin lives in one file. |
| S3 | List-page filter plumbing (`patchParams`, debounced fetch, sort, pagination reset in `Locations.jsx`) will be duplicated across species/permits — extract a `useUrlFilters` hook + filter toolbar composition. | **Fixed 2026-07-02** — `src/hooks/useUrlFilters.js` (URL patch/paging, pure `applyParamPatch` helper with tests) and `src/hooks/useDebouncedQuery.js` (debounced fetch with stale-result guard) now drive all three list pages. Permits was converted from the old submit-based `SearchFilter` to the same auto-apply URL-driven pattern (`?q=`), which made `SearchFilter` dead code (removed in S14). |
| S4 | Consider feature folders (`src/features/{location,species,permit}/` with `pages/`, `components/`, `api.js`), keeping `src/components/ui/` for generic UI only. Do incrementally, one domain at a time. | Open |
| S5 | Minimal test suite: pure logic is very testable (`Pagination.fetchPageNumbers`, `patchParams`, services against mocked fetch). | Mostly done 2026-07-02 — 38 tests across 6 files: `apiClient`, `authService.getSession`, `locationService` (query-string building incl. the radius-needs-origin rule, FormData payloads, delete outcomes), `geoUtils`, `applyParamPatch`, `findMissingEnvVars`. Vitest `test.env` in `vite.config.js` provides deterministic `VITE_` values. Component-level tests would need @testing-library — not set up. |
| S6 | `src/hooks/useLocation.jsx` shadows react-router's `useLocation` — rename to `useGeolocation`. Geolocation also doesn't belong in `CurrentUserContext`. | **Fixed 2026-07-02** — renamed to `src/hooks/useGeolocation.js`. The remaining `useLocation` references are react-router's own (Header, ScrollToTop). Geolocation staying in `CurrentUserContext` is accepted for now — it's app-wide shared state and works. |
| S7 | Non-component files (`services/*.jsx`, `utils/geoUtils.jsx`, `hooks/*.jsx`) contain no JSX — rename to `.js`. | **Fixed 2026-07-02** — all six services, `geoUtils`, `useData`, and the geolocation hook renamed to `.js` via `git mv` (history preserved). All imports were extensionless, so no import edits were needed. |
| S8 | Validate the four required `VITE_` env vars at startup (fail fast in `index.jsx`) instead of silent runtime failures. | **Fixed 2026-07-02** — `src/utils/assertEnv.js` runs before render: missing/empty vars render a readable error box into #root and throw. Verified live by booting with `VITE_BASE_URL=""`. Pure `findMissingEnvVars` helper has tests. |
| S9 | Shared constants duplicating: valid image-type lists were copied (with drift — one accepted `image/x-icon`) across `DragAndDropImage`, `MediaManagerPanel`, `StepPhotos`, and `AddSpecies`; image URL building was inlined in five components. | **Fixed 2026-07-02** — `src/constants/images.js` (`VALID_IMAGE_TYPES`, `IMAGE_ACCEPT`) used by all four uploaders; URL building centralized in `fileService.getImageUrl`. Per-domain `MAX_IMAGES` values (5 locations / 4 species) intentionally stay local. |
| S10 | Dead code: commented-out helpers in `DragAndDropImage.jsx`, leftover "Refactor" comment in `geoUtils.jsx`, no-op `exact` props on React Router v7 routes in `App.jsx`. | **Fixed 2026-07-02** — commented-out helpers went with the S14 deletion, the stale comment is now a real JSDoc on `getGeometryCenter`, and the six `exact` props are removed from `App.jsx`. |
| S14 | **Dead component chain discovered 2026-07-02:** no page imports `SpeciesForm`, `LocationForm`, or `LocationFormCard` anymore — the redesigned wizard/edit panels replaced them — making a subtree of components unreachable. | **Fixed 2026-07-02** — import-graph reachability trace from `src/index.jsx` found 23 unreachable files, all deleted: `SpeciesForm`, `SpeciesFilter`, `LocationForm`, `LocationFormCard`, `DragAndDropImage`, `ArticleInput`, `MapInput`, `CollapsibleTextarea`, `CheckBox`, `Select`, `Form`, `ButtonPrimaryOutline`, `MapMarker`, `SearchFilter` (+ their SCSS). Re-trace after deletion: zero unreachable files; build and all routes verified. Note `MultiSelect`, `Range`, `TextArea`, `LocationGeometryInput` turned out to be alive (used by the map page's `LocationFilter` and the geometry editor). |
| S11 | No lint script — add `"lint": "eslint src"` and run it in CI. | **Fixed 2026-07-02** — the "react-app preset" mentioned in the old docs turned out not to exist (no eslint install or config at all — a CRA-era leftover). Set up ESLint 9 flat config (`eslint.config.js`: js recommended + react + react-hooks v7), `npm run lint`, and a Lint step in the workflow between install and build. Fixed all 19 initial errors (mirror-ref writes during render, declaration-order issues, a case-block declaration) and cleaned ~20 unused-var warnings; `react-hooks/refs` + `set-state-in-effect` are downgraded to warnings for the imperative Maps code (12 advisory warnings remain, 0 errors). |
| S12 | Deploy uploads the entire repo (incl. `node_modules`) as the artifact; consider deploying only `build/` + whatever the Azure app needs at runtime. | **Fixed 2026-07-02** — artifact is now the `build/` folder only, re-nested under `build/` on download so the Azure app's serving path (wwwroot/`build/`, where the SPA rewrite `web.config` lands via `public/`) is preserved. **Verify the first deploy after pushing** — this assumes the app's physical path points at the `build/` subfolder (the only configuration consistent with the repo: no root web.config, no server entry point, root index.html is the raw Vite dev shell). |
| S13 | Error-handling UX: services swallow errors, so users get no feedback on failures — consider a small toast/notification system. | **Fixed 2026-07-02** — `src/context/ToastContext.jsx` (`useToast()`, error/success, auto-dismiss + manual close, `aria-live`) mounted in `App.jsx`. Wired: AddLocation/AddSpecies/AddPermit/EditPermit create/save failures, EditPermit delete (no longer navigates away on a failed delete), and EditUser details/password (success + failure — previously fully silent). Verified live against real backend 401s. Edit panels and login keep their inline status UI. |
